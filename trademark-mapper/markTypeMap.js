/**
 * Maps type of mark to protect to mark format.
 * Lookup is case-insensitive (convert to lowercase before lookup).
 */
export const typeOfMarkToProtectMap = {
	'typed (standard characters)': 'standard_character',
	'design (special form - stylized and/or design)': 'design_mark',
};

